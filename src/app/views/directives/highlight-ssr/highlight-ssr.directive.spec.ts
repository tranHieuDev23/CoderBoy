import { HighlightSsrDirective } from './highlight-ssr.directive';

describe('HighlightSsrDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightSsrDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
