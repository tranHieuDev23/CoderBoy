import { TwitterHandleUrlPipe } from './twitter-handle-url.pipe';

describe('TwitterHandleUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new TwitterHandleUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
