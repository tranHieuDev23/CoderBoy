import { ExtractUrlPipe } from './extract-url.pipe';

describe('ExtractUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
